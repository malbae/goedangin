// next.config.mjs
const nextConfig = {
  images: {
    domains: ['tailwindui.com', 'via.placeholder.com', 'blog.logomaster.ai'], // Tambahkan domain yang diperlukan di sini
  },
}

export default nextConfig; // Gunakan ini jika `next.config.mjs` menggunakan ES Modules
